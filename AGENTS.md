# AGENTS.md

## 프로젝트 원칙

- 로그인 전 화면과 로그인 후 할 일 화면을 각각 한 화면으로 유지한다.
- `PRD.md`와 `design.md`에 없는 기능은 먼저 추가하지 않는다.
- 중요도는 사용자가 직접 고르는 `높음 / 보통 / 낮음` 3단계만 사용한다.
- 1주차에는 중요도 설정 기준이 먼저 보이게 하고, 기준 없이 선택하게 만들지 않는다.
- 로그인 후 화면에는 사용자 표시, 입력칸, 중요도 선택, 카카오 안내, 정렬 목록만 둔다.
- 정해지지 않은 것은 임의로 만들지 않는다.
- 순수 HTML, CSS, JavaScript와 Node.js 기본 모듈만 사용하고 프레임워크나 외부 패키지를 추가하지 않는다.
- 카카오 실제 로그인은 `server.js`를 통해 실행하며 토큰과 Client secret을 브라우저 저장소에 두지 않는다.
- 핵심 기능 추가·수정·제거가 끝나면 `memory.md`에 KST 일시와 변경 파일을 기록한다.

## AI 개발팀

- `a팀-html담당`: HTML 담당. 모든 HTML 파일의 문서 구조, 시맨틱 마크업, 접근성을 맡는다.
- `b팀-css담당`: CSS 담당. `styles.css`의 레이아웃, 반응형 디자인, 색상, 애니메이션을 맡는다.
- `c팀-js담당`: JavaScript 담당. 브라우저 JavaScript와 `server.js`의 OAuth, 세션, 저장, 기능 동작을 맡는다.
- `b팀-QA엔지니어`: QA 엔지니어. 변경 사항과 기존 기능을 읽기 전용으로 검증하고 결함을 보고한다.

## 메인 에이전트 위임 규칙

- 메인 에이전트는 요청을 분석하고 담당 파일에 맞는 전문 에이전트에게 작업을 위임한다.
- HTML 변경은 `a팀-html담당`, CSS 변경은 `b팀-css담당`, 브라우저·서버 JavaScript 변경은 `c팀-js담당`에게 맡긴다.
- 코드 변경이 끝나면 `b팀-QA엔지니어`에게 구문 오류, 화면 범위, 기존 저장·정렬·완료·미루기 기능 회귀를 확인하게 한다.
- 여러 영역이 필요한 작업은 담당 에이전트를 순서대로 호출한다. 같은 작업 폴더에서 여러 쓰기 에이전트가 동시에 파일을 수정하게 하지 않는다.
- 메인 에이전트는 요구사항 결정, 에이전트 결과 통합, 충돌 해결, 최종 설명, `memory.md` 기록을 책임진다.
- 담당 밖 변경이 필요하면 전문 에이전트가 직접 수정하지 않고 메인 에이전트에 필요한 변경을 보고한다.
- 사용자가 특정 에이전트를 지목하면 해당 에이전트를 우선 호출한다.

## 공통 완료 기준

- `npm start` 후 `http://localhost:3000`에서 로그인 화면이 열린다.
- 로그인 후 한 화면에서 사용자 표시, 입력, 중요도, 카카오 안내, 정렬 목록이 모두 보인다.
- 카카오 토큰과 Client secret이 HTML, URL, 로그, `localStorage`에 노출되지 않는다.
- 사용자별 할 일 저장소가 분리된다.
- 기존 저장, 중요도 정렬, 완료, 하루 미루기 기능이 유지된다.
- 정해지지 않은 기능과 기준을 임의로 추가하지 않는다.
- 변경한 파일과 검증 결과를 메인 에이전트에 간결하게 보고한다.

## 참고 자료

- `PRD.md`, `design.md`, `index.html`, `login.html`, `styles.css`, `app.js`, `login.js`, `server.js`, `memory.md`, 사용자 메모를 사용한다.
- 자료가 없거나 결정되지 않은 내용은 추측하지 말고 메인 에이전트에 알린다.
