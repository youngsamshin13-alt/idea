# DESIGN: 아침 카카오 우선순위 알림 서비스

## 1. 문서 목적

이 문서는 `PRD.md`에 정리된 핵심 기능을 바탕으로, 초보자도 따라 만들 수 있게 화면의 분위기와 배치 기준을 정리한 디자인 가이드다.

이 문서의 목적은 기능을 더 많이 넣는 것이 아니라, 지금 정한 핵심 기능이 더 쉽고 또렷하게 보이게 만드는 것이다.

## 2. 디자인 한 줄 정의

퇴근길에 지친 사용자가 복잡하게 생각하지 않아도, 할 일을 빠르게 적고 다음 날 아침 꼭 다시 보게 해주는 차분한 도우미 같은 화면

## 3. 이 서비스가 주어야 하는 느낌

- 서두르게 하지 않는 차분함
- 한눈에 이해되는 분명함
- 급한 일이 먼저 보이는 정리감
- 혼내지 않고 도와주는 친절함

## 4. 초보자를 위한 디자인 원칙

1. 한 화면에는 가장 중요한 행동 1개만 강하게 보이게 한다.
2. 사용자가 많이 입력하지 않도록, 직접 쓰는 칸보다 고르는 칸을 더 쉽게 만든다.
3. 급한 일은 색보다 먼저 순서로 보여준다.
4. 설명 문장은 짧고 부드럽게 쓴다.
5. 버튼은 많게 두지 말고, 가장 중요한 버튼 1개만 진하게 보여준다.

## 5. 첫 화면의 역할

로그인 후 첫 화면은 아래 4가지만 바로 할 수 있으면 된다.

- 할 일을 적는다.
- 중요도를 정한다.
- 아침 카카오톡 알림이 간다는 점을 확인한다.
- 등록된 할 일이 급한 순서대로 보인다.

### 5.1 로그인 전 화면

- 로그인 전에는 중앙에 로그인 카드 하나만 보여준다.
- 제목, 짧은 안내 문구, `카카오로 계속하기` 버튼 한 개만 강하게 보여준다.
- 카카오 아이디와 비밀번호 입력칸은 만들지 않는다.
- 로그인 실패나 취소 안내는 버튼 가까이에 짧고 부드럽게 표시한다.
- 기존 메인 색, 카드 배경, 둥근 모서리와 그림자를 재사용한다.
- 로그인 카드 최대 너비는 `440px`로 하고 모바일에서는 좌우 여백 `16px`를 유지한다.
- 키보드 포커스가 분명해야 하며 모션 최소화 설정을 존중한다.

### 5.2 로그인 후 사용자 표시

- 할 일 화면 위쪽에 카카오 사용자 이름과 `로그아웃` 버튼을 작게 표시한다.
- 사용자 표시는 할 일 저장 버튼보다 강하게 보이지 않게 한다.
- 프로필 정보가 없으면 `카카오 사용자`라고 표시한다.

## 6. 첫 화면 구성

### 6.1 화면 위쪽

- 짧은 안내 문구가 보인다.
- 추천 문구: `내일 아침, 급한 일부터 다시 알려드릴게요`
- 너무 긴 설명은 넣지 않는다.

### 6.2 할 일 입력 영역

- 화면에서 가장 먼저 보이는 큰 입력칸이 있다.
- 예시 문구: `내일 꼭 해야 할 일을 적어보세요`
- 입력칸 아래에는 작은 도움말 한 줄만 둔다.
- 추천 문구: `퇴근길에 생각난 일을 짧게 적어도 괜찮아요`

### 6.3 중요도 선택 영역

- 입력칸 바로 아래에 중요도를 고르는 버튼이 보인다.
- 추천 방식: `높음 / 보통 / 낮음`
- 선택된 값은 색이 채워지고, 선택되지 않은 값은 테두리만 보이게 한다.

### 6.4 카카오톡 알림 안내 영역

- 별도 설정 화면으로 빼기보다, 첫 화면 안에서 짧게 보여준다.
- 추천 문구: `다음 날 출근 전에 카카오톡으로 알려드려요`
- 아직 정확한 시간 입력 여부가 정해지지 않았으므로, 시간 입력칸은 지금 넣지 않는다.

### 6.5 저장 버튼

- 화면에서 가장 강하게 보여야 하는 버튼이다.
- 추천 문구: `내일 할 일 저장`
- 버튼은 한 개만 진한 색으로 보여준다.

### 6.6 정리된 할 일 목록 영역

- 저장된 할 일은 아래쪽 리스트에 바로 보인다.
- 가장 급한 일이 맨 위에 보인다.
- 각 항목에는 `할 일 내용`과 `중요도`만 우선 보여준다.
- 아직 사용자가 직접 요청하지 않은 세부 기능은 붙이지 않는다.

## 7. 추천 화면 흐름

초보자가 만들 때는 아래 순서대로 화면을 잡으면 덜 헷갈린다.

1. 상단 안내 문구를 둔다.
2. 가장 큰 영역으로 할 일 입력칸을 둔다.
3. 바로 아래에 중요도 선택 버튼을 둔다.
4. 그 아래에 카카오톡 알림 안내 문구를 둔다.
5. 마지막에 정리된 할 일 목록을 둔다.

## 8. 추천 와이어프레임

```text
[내일 아침, 급한 일부터 다시 알려드릴게요]

[ 내일 꼭 해야 할 일을 적어보세요                  ]
[ 퇴근길에 생각난 일을 짧게 적어도 괜찮아요        ]

[ 높음 ] [ 보통 ] [ 낮음 ]

[ 다음 날 출근 전에 카카오톡으로 알려드려요 ]

[ 내일 할 일 저장 ]

오늘 정리된 할 일
1. 병원 예약 전화   높음
2. 보고서 수정      높음
3. 세탁 맡기기      보통
```

## 9. 색상 가이드

색은 많지 않게 쓰는 편이 좋다. 이 서비스는 불안하게 만들기보다 정리해주는 느낌이 중요하다.

- 메인 색: `#F4B400`
- 보조 색: `#FFF4CC`
- 글자 진한 색: `#222222`
- 글자 연한 색: `#6B7280`
- 배경색: `#FFFDF8`
- 카드 배경: `#FFFFFF`
- 테두리 색: `#E7E1D6`
- 중요도 높음 표시: `#E86A33`
- 중요도 보통 표시: `#F4B400`
- 중요도 낮음 표시: `#8AA399`

추천 이유는 카카오톡 느낌과 자연스럽게 어울리면서도, 너무 강한 경고 화면처럼 보이지 않기 때문이다.

## 10. 글자 가이드

초보자라면 글꼴과 크기를 많이 섞지 않는 것이 좋다.

- 추천 글꼴: `Pretendard` 또는 `Noto Sans KR`
- 화면 제목: `24px`, 굵게
- 안내 문구: `16px`
- 입력칸 글자: `16px`
- 버튼 글자: `16px`, 굵게
- 목록 제목: `18px`, 굵게
- 목록 내용: `15px`

원칙은 `제목 크고`, `입력 읽기 쉽고`, `설명 짧고 연하게`다.

## 11. 여백과 크기 가이드

정리된 느낌은 색보다 여백에서 더 많이 나온다.

- 화면 바깥 여백: `20px`
- 카드 사이 간격: `16px`
- 입력칸 높이: `56px` 이상
- 버튼 높이: `52px` 이상
- 버튼 둥글기: `14px`
- 카드 둥글기: `16px`

너무 많은 선과 박스를 넣지 말고, 여백으로 구분한다.

## 12. 버튼과 입력칸 스타일

### 버튼

- 메인 버튼은 노란색 계열로 채운다.
- 버튼 안 글자는 진한 검정이나 아주 어두운 갈색으로 둔다.
- 버튼 그림자 효과는 약하게만 넣는다.

### 입력칸

- 흰 배경 위에 옅은 테두리를 둔다.
- 입력 전에는 예시 문구가 보인다.
- 사용자가 누르면 테두리 색이 메인 색으로 바뀌게 한다.

### 중요도 선택 버튼

- 칩 형태의 작은 둥근 버튼으로 만든다.
- 선택된 버튼만 배경색이 채워지게 한다.
- 중요도별 색이 다르더라도 톤은 부드럽게 유지한다.

## 13. 문구 톤 가이드

이 서비스의 문구는 관리당하는 느낌보다 도와받는 느낌이 중요하다.

- 좋은 예: `내일 아침에 다시 알려드릴게요`
- 좋은 예: `급한 일부터 정리해두었어요`
- 좋은 예: `짧게 적어도 괜찮아요`
- 피해야 할 예: `반드시 입력하세요`
- 피해야 할 예: `지금 바로 처리하지 않으면 놓칠 수 있습니다`

## 14. 꼭 보여야 하는 정보

첫 화면에서 사용자가 바로 이해해야 하는 정보는 아래 4개다.

- 무엇을 적는 화면인지
- 중요도를 어디서 고르는지
- 카카오톡으로 다시 알려준다는 점
- 지금까지 적은 일이 급한 순서대로 보인다는 점

이 4개가 흐려지면 화면은 예뻐 보여도 서비스의 핵심이 약해진다.

## 15. 지금 넣지 않는 표현

아래 내용은 아직 결정되지 않았으므로 디자인에도 먼저 넣지 않는다.

- 정확한 알림 시간 입력칸
- 완료 버튼
- 미루기 버튼
- 재알림 간격 선택
- 카카오톡 외 다른 알림 수단

## 16. 초보자가 그대로 따라 하는 작업 순서

1. 먼저 첫 화면에 `입력칸`, `중요도 선택`, `카카오 안내`, `저장 버튼`, `목록` 다섯 덩어리만 놓는다.
2. 그다음 색을 넣기 전에 흑백으로 먼저 배치를 맞춘다.
3. 배치가 정리되면 메인 색 한 개와 보조 색 한 개만 넣는다.
4. 가장 중요한 버튼 하나만 진하게 만든다.
5. 마지막에 문구를 짧고 부드럽게 다듬는다.

## 17. 디자인 체크리스트

- 첫 화면만 보고도 무엇을 하는 서비스인지 알 수 있는가
- 사용자가 3초 안에 입력칸을 찾을 수 있는가
- 급한 일이 맨 위에 보이는가
- 버튼이 너무 많지 않은가
- 설명이 길어서 읽기 싫어지지 않는가
- 경고 화면처럼 부담스럽지 않은가

## 18. 이 문서 기준으로 다음에 만들 수 있는 것

이 `design.md` 다음 단계에서는 아래 작업으로 이어가기 좋다.

- 첫 화면 목업 만들기
- 모바일 화면 시안 만들기
- 버튼, 입력칸, 카드 스타일 정리
- 카카오톡 알림 메시지 시안 만들기

## 19. 아직 남아 있는 질문

아래는 디자인 전에 꼭 확정할 필요는 없지만, 다음 단계에서 정하면 더 좋아지는 질문이다.

- 중요도는 사용자가 직접 고르는 방식으로 확정할지
- 중요도를 `높음 / 보통 / 낮음` 3단계로 확정할지
- 카카오톡 알림 시간을 사용자가 직접 정하는지
- 첫 버전에 완료와 미루기 기능을 넣는지
